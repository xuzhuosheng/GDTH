<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gdtheurekaproviderywgn.dao.RjywglDao">

    <!--<select id="getUserData" resultType="com.example.gdtheurekaproviderxtgn.entity.User">-->
    <!--select * from user-->
    <!--<where>-->
    <!--1=1-->
    <!--and account=#{username}-->
    <!--and password=#{password}-->
    <!--</where>-->
    <!--</select>-->

    <!--<select id="getAllUsers" resultType="com.example.gdtheurekaproviderxtgn.entity.User">-->
    <!--select * from user-->
    <!--</select>-->


    <insert id="insertRjywjl">
        insert into rjywgl
        (type, title, content, scripttxt, creator, createtime, bz)
          values (
        #{type},#{title},#{content},#{scripttxt},#{creator},sysdate,#{bz}
        )
    </insert>
</mapper>